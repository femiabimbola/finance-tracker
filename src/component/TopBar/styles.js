import { styled } from '@mui/system';
import {AppBar} from "@mui/material";

const AppBarStyle = styled(AppBar)({
  color: 'black',
  backgroundColor: '#d2efc2',
  padding: 10,
  margin:"0 10",
  borderRadius: 4,
  alignItems:'center',
  position:'static'
});

export default AppBarStyle;